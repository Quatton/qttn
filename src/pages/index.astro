---
import { getAppHref } from "@/lib/utils";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <main class="container mx-auto h-[100dvh] pt-16">
    <section class="max-w-3xl px-8 py-32">
      <div class="relative z-10 p-8">
        <div
          class="absolute inset-x-0 inset-y-6 -z-10 translate-y-12 rounded-xl bg-secondary"
        >
        </div>

        <h1 class="z-20 text-pretty text-3xl font-bold md:text-4xl">
          <span>Your problem,</span><br />
          <span class="text-secondary-content">others might have it too.</span>
        </h1>
        <p class="z-20 mt-4 text-pretty text-lg text-secondary-content">
          Hi, I'm Quatton. I like making things for my friends and family to
          use.
        </p>
        <p class="z-20 mt-2 text-pretty text-lg text-secondary-content">
          The process is simple, request me to build stuff. In return, let me
          publish or open-source it for others to use as well!
        </p>
      </div>

      <a class="btn btn-outline mt-12" href={getAppHref("blog")}>
        Stuff I wrote
      </a>
    </section>
    <section class="max-w-3xl px-8 py-32">
      <h2 class="text-3xl font-semibold">Recent Projects</h2>
      <div class="mt-8 grid grid-cols-1 gap-8">
        <div class="border-box card border">
          <div class="card-body">
            <h3 class="card-title">CONST!</h3>
            <p class="mt-4 text-lg text-secondary-content">
              Const is a collection of fun constrained writing prompts for you
              to play and share with your friends.
            </p>

            <div class="card-actions justify-end">
              <a href={getAppHref("const")} class="btn btn-sm"> Let's Play </a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>
