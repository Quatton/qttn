---
import { site } from "@/consts/config";
import { cn } from "@/lib/utils";
import { type ClassValue } from "clsx";

interface Props {
  class?: ClassValue;
}

const className = Astro.props.class || "";
---

<header class={cn("p-4", className)}>
  <a href="/" class="text-2xl font-bold font-serif">
    {site.title}
  </a>
</header>
